# SQLite (local / single-server deploy) — the default
DATABASE_URL="file:./dev.db"

# To deploy on a managed database instead, switch prisma/schema.prisma
# `provider` to "postgresql" and set a connection string, e.g.:
# DATABASE_URL="postgresql://user:password@host:5432/eteaching"
